LAB3 - Shell 
Features that work:
Pipes, (most) file redirection, isatty(), ctrl-c, zombie elimination, exit, tilde expansion, edit mode, history

Features that mostly work:
environmental variable expansion, builtin function

Features that seem like they should work but are mostly broken:
subshell, quotes, escaping

Features that do not work:
path completion, wildcarding, creating default source

